#!/bin/bash
name="jethalal"

if [ $name = "jethalal" ]; then
    echo "$name is loyal"
else
    echo "Not Loyal"
fi

# In Linux shell scripting, fi is simply the end marker for an if statement.
# if → starts the conditional block.
# fi → ends it (it's just "if" written backwards).

